#include "Base.h"

using namespace L;
using namespace DBMS;
/*
Base::Base(const Directory& dir){
    Directory::Content dc(dir.content());
    L_Iter(dc.files,it)
        table.insert(make_pair(it->name(),Table(*it)));
}
void Base::query(const String& sql){
    // CREATE
        // INDEX
        // TABLE
    // DROP
        // INDEX
        // TABLE
    // SELECT
        // FROM
}
*/
